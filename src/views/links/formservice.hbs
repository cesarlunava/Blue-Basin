{{> navigation}}
<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    {{#if links}}
                    <form action="/sisris/edit/{{links.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" name="nombre" value="{{links.nombre}}" class="form-control" placeholder="Nombre del Servicio" autofocus="true">
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{links.descripcion}}" name="descripcion" rows="2" class="form-control" placeholder="Descripción">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block" type="submit">Editar SISRI</button>
                        </div>
                    </form>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>

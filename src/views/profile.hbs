{{> navigation}}

<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    {{#if usuario}}
                    <h3>Buen día, {{usuario.nombre}}</h3>
                    <h4>Bandeja de Entrada</h4>
                    {{#each sisri}}
                    <div class="col-md-1 mx-auto">
                        <div class="card-body">
                            <p>{{usuario.nombre}}, ¡su nuevo <a href="/sisris/">servicio</a> ya esta en línea!</p>
                            <p>{{sisri.created_at}}</p>
                        </div>
                    </div>
                    {{/each}}
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>
